# Web 워크벤치 — 세로 슬라이스 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 업로드 → 시리즈 선택(게이트) → 파이프라인(백그라운드) → 변형 하나 → 단일 뷰어(불투명/반투명 + 지표)로 4파일을 실제로 "딸깍" 산출하는 최소 완결 흐름.

**Architecture:** 단일 사용자·로컬·한 번에 잡 하나. 큐·DB 없음 — 잡은 폴더 하나 + `status.json`이다. FastSurfer(2.5분)는 백그라운드 스레드로 돌리고 UI는 `status.json`을 폴링한다. 기존 모듈(io·segment·labels·mesh)을 파이프라인이 순서대로 호출하는 글루다.

**Tech Stack:** FastAPI, uvicorn, three.js(뷰어). 기존: nibabel, 그리고 io·segment·labels·mesh 패키지.

## Global Constraints

- 스펙 §5.1: FastSurfer는 docker socket으로 형제 컨테이너를 띄운다. 로컬 단독·비노출 전제. 이 트리거는 `segment` 인터페이스 하나 뒤에 격리한다(이미 `segment.run_fastsurfer`가 그 지점).
- **형제 컨테이너 경로:** 도커 데몬은 호스트에 있다. api 컨테이너가 `docker run -v <경로>`를 넘길 때 `<경로>`는 **호스트 경로**여야 한다. 컨테이너 경로↔호스트 경로 변환은 `jobs.layout.to_host_path`(Task 1) 하나에만 있고, `SAM_HOST_JOBS_ROOT` 환경변수로 짝을 알려준다. **로컬 파일을 읽고 쓰는 코드는 절대 호스트 경로를 쓰지 않는다** — `-v` 인자에만 쓴다.
- **로컬호스트 밖과 통신하지 않는다.** (a) 런타임에 외부 네트워크를 타지 않는다 — three.js는 저장소에 vendor한다(Task 6), CDN 요청 금지. (b) compose는 포트를 `127.0.0.1:${WEB_PORT}:8000`으로만 매핑한다(Task 7) — LAN에 노출하지 않는다. (c) 컨테이너 안 uvicorn은 `--host 0.0.0.0`이 필요하지만(그래야 호스트→컨테이너 포트 매핑이 닿는다) 노출은 (b)의 127.0.0.1 매핑이 막는다. 호스트에서 uvicorn을 직접 돌리는 경우엔 `--host 127.0.0.1`로 띄운다 — 0.0.0.0으로 두면 LAN에 열린다. 환자 데이터를 다루는 로컬 단독 툴이다.
- 스펙 §6.3: **시리즈 자동 선택만으로 진행하지 않는다. 사용자 확인을 받는다.** series 선택은 실 UI 게이트다 — 자동 시작 금지.
- 스펙 §12: `status.json`의 `error`에 **원본 파일명·경로·DICOM 태그를 넣지 않는다**(PHI 유출). 실패 단계 + 종료코드 + stderr 마지막 줄(경로 없는)만.
- 스펙 §2: 케이스당 4파일 — `orig.nii.gz`(uint8), `seg.nii.gz`(uint8 canonical), `regions.glb`(노드 `label_<id>`), `regions-meta.json`.
- 스펙 §2.2: `regions-meta.json`은 v2. 헤더에 `space.affine`(native), `source`, `labelTable`, `meshVariantId`. 색·group·side·centroid는 regions 배열이 운반(뷰어가 칠함, B안).
- 스펙 §6.4: `--no_cc` 절대 금지(segment가 이미 보장). withCC 세그.
- 스펙 §11: 저장소에는 `.env.example`만. `.env`는 커밋 금지(.gitignore에 이미 있음). 키: `OUTPUT_DIR`(필수), `FASTSURFER_IMAGE`(필수, latest 금지), `FASTSURFER_THREADS`(8), `MAX_UPLOAD_MB`(4096), `MAX_EXTRACT_GB`(20), `WEB_PORT`(8000).
- 스펙 §7: `variantId`는 `v<순번 2자리>-<해시4>`. 잡 디렉터리 구조는 §7 그대로.
- 테스트는 `uv run pytest -W error`로 통과. FastSurfer는 목(스펙 §13).

## 범위 밖 (todo — 나중 붙여도 재작업 없음)

- **정본 확정**(§6.6 `configs/canonical-mesh-v1.json`, `out/` 사본) — 탐색이 먼저다. `meshConfig`는 지금 null.
- **스토리지 관리 UI/API**(§9.2·§9.3) — 단일 사용자엔 과함. 폴더 지우면 끝.
- **좌우 2-up 비교 + 카메라 동기**(§8) — 단일 뷰어 먼저. 2-up은 fast-follow.
- **변형 해시 중복감지**(§7), **잡 취소**(컨테이너 kill), **압축**(meshopt), **PDF**.
- **`space.toMNI152` 어파인**(§2.2) — brain-educate 뇌간 임시해 있음. SimpleITK 등록은 별도.
- 여러 변형을 UI에서 축 조합으로 만드는 패널 — 이 슬라이스는 기본 변형 하나 + "변형 추가" 엔드포인트 하나. 축 조합 UI는 fast-follow.

## File Structure

```
seg_and_mesh/
  jobs/
    __init__.py
    layout.py      잡 디렉터리 경로 (§7)
    status.py      status.json 읽기/쓰기, 상태 전이, PHI-안전 에러 기록
    meta.py        regions-meta.json 조립 (§2.2)
    pipeline.py    io→segment→remap→mesh 순서 실행 (동기 코어) + 백그라운드 러너
  web/
    __init__.py
    app.py         FastAPI 라우트 + 정적 파일 서빙
    static/
      index.html   뷰어 페이지
      viewer.js     three.js 뷰어 (단일 변형, 불투명/반투명, 지표)
docker/
  api.Dockerfile   CMD를 uvicorn으로 (수정)
docker-compose.yml (신규)
.env.example       (신규)
tests/
  jobs/{__init__,test_layout,test_status,test_meta,test_pipeline}.py
  web/{__init__,test_app}.py
```

---

## Task 1: 의존성 + 잡 디렉터리 레이아웃

**Files:**
- Modify: `pyproject.toml`, `uv.lock`
- Create: `seg_and_mesh/jobs/__init__.py`, `seg_and_mesh/jobs/layout.py`
- Test: `tests/jobs/__init__.py`, `tests/jobs/test_layout.py`

**Interfaces:**
- Produces:
  - `new_job_id(now: datetime | None = None) -> str` — `YYYY-MM-DD-HHMMSS-<hex4>` (스펙 §9.1 예: `2026-07-22-143011-a3f9`)
  - `@dataclass(frozen=True) JobPaths` — 필드 `root: Path`, 프로퍼티 `input_dir`, `nifti_dir`, `fs_dir`, `seg_dir`, `mesh_dir`, `out_dir`, `status_file`, 메서드 `variant_dir(variant_id: str) -> Path`, `create() -> "JobPaths"`(모든 하위 폴더 생성)
  - `job_paths(jobs_root: Path, job_id: str) -> JobPaths`
  - `to_host_path(path: Path, *, jobs_root: Path, host_jobs_root: Path | None) -> Path` — 컨테이너 경로를 호스트 경로로 옮긴다. 아래 "형제 컨테이너 경로" 참고.

**형제 컨테이너 경로 (반드시 읽을 것):**

api는 컨테이너 안에서 돌고, FastSurfer는 `docker run`으로 뜨는 **형제 컨테이너**다. `-v <경로>:/output`의 `<경로>`는 도커 데몬이 해석하는데 **데몬은 호스트에 있다.** 그래서 api가 자기 눈에 보이는 `/work/jobs/...`를 넘기면 데몬이 호스트에서 그 경로를 못 찾아 빈 폴더를 마운트한다.

compose가 `${OUTPUT_DIR}:/work/jobs`로 바인드하므로, 같은 파일이 호스트에선 `${OUTPUT_DIR}/...`, api 안에선 `/work/jobs/...`다. `to_host_path`가 그 접두사를 바꿔치기한다.

`host_jobs_root`가 `None`이면(= 호스트에서 직접 실행하는 개발 모드) 경로를 그대로 돌려준다. `path`가 `jobs_root` 밑이 아니면 `ValueError`.

```python
def to_host_path(path, *, jobs_root, host_jobs_root):
    """컨테이너 경로 -> 호스트 경로 (형제 컨테이너 -v 인자용).

    도커 데몬은 호스트에 있어 api 컨테이너 내부 경로를 모른다. compose가
    host_jobs_root를 jobs_root에 바인드했다는 전제로 접두사를 바꾼다.
    host_jobs_root가 None이면(호스트 직접 실행) 그대로 둔다.
    """
    path = Path(path)
    if host_jobs_root is None:
        return path
    rel = path.relative_to(jobs_root)   # 밖이면 ValueError
    return Path(host_jobs_root) / rel
```

테스트에 넣을 것: (a) `host_jobs_root=None`이면 항등, (b) 접두사 치환이 맞음, (c) `jobs_root` 밖 경로는 `ValueError`, (d) `jobs_root`와 같은 경로면 `host_jobs_root` 자신.

- [ ] **Step 1: `tests/jobs/__init__.py`를 빈 파일로 만든다**

- [ ] **Step 2: 실패하는 테스트를 쓴다**

`tests/jobs/test_layout.py`:

```python
"""잡 디렉터리 레이아웃 (스펙 §7, §9.1)."""

from __future__ import annotations

import re
from datetime import datetime

from seg_and_mesh.jobs.layout import JobPaths, job_paths, new_job_id


def test_job_id_format():
    jid = new_job_id(datetime(2026, 7, 22, 14, 30, 11))
    assert re.fullmatch(r"2026-07-22-143011-[0-9a-f]{4}", jid)


def test_job_ids_are_unique():
    now = datetime(2026, 7, 22, 14, 30, 11)
    assert new_job_id(now) != new_job_id(now)  # 랜덤 접미가 다르다


def test_paths_follow_spec_layout(tmp_path):
    p = job_paths(tmp_path, "2026-07-22-143011-a3f9")
    assert p.root == tmp_path / "2026-07-22-143011-a3f9"
    assert p.input_dir.name == "input"
    assert p.nifti_dir.name == "nifti"
    assert p.fs_dir.name == "fs"
    assert p.seg_dir.name == "seg"
    assert p.mesh_dir.name == "mesh"
    assert p.out_dir.name == "out"
    assert p.status_file.name == "status.json"
    assert p.variant_dir("v01-3c81") == p.mesh_dir / "v01-3c81"


def test_create_makes_all_dirs(tmp_path):
    p = job_paths(tmp_path, "job1").create()
    for d in (p.input_dir, p.nifti_dir, p.fs_dir, p.seg_dir, p.mesh_dir, p.out_dir):
        assert d.is_dir()
```

- [ ] **Step 3: 실패를 확인한다**

Run: `uv run pytest tests/jobs/test_layout.py -q`
Expected: `ModuleNotFoundError: No module named 'seg_and_mesh.jobs'`

- [ ] **Step 4: 구현한다**

`seg_and_mesh/jobs/layout.py`:

```python
"""잡 디렉터리 레이아웃 (스펙 §7).

단일 사용자·로컬이라 잡은 폴더 하나다. 큐·DB 없음.
"""

from __future__ import annotations

import secrets
from dataclasses import dataclass
from datetime import datetime
from pathlib import Path


def new_job_id(now: datetime | None = None) -> str:
    """YYYY-MM-DD-HHMMSS-<hex4> (스펙 §9.1)."""
    now = now or datetime.now()
    return f"{now:%Y-%m-%d-%H%M%S}-{secrets.token_hex(2)}"


@dataclass(frozen=True)
class JobPaths:
    """잡 하나의 경로들 (스펙 §7)."""

    root: Path

    @property
    def input_dir(self) -> Path:
        return self.root / "input"

    @property
    def nifti_dir(self) -> Path:
        return self.root / "nifti"

    @property
    def fs_dir(self) -> Path:
        return self.root / "fs"

    @property
    def seg_dir(self) -> Path:
        return self.root / "seg"

    @property
    def mesh_dir(self) -> Path:
        return self.root / "mesh"

    @property
    def out_dir(self) -> Path:
        return self.root / "out"

    @property
    def status_file(self) -> Path:
        return self.root / "status.json"

    def variant_dir(self, variant_id: str) -> Path:
        return self.mesh_dir / variant_id

    def create(self) -> "JobPaths":
        for d in (self.input_dir, self.nifti_dir, self.fs_dir,
                  self.seg_dir, self.mesh_dir, self.out_dir):
            d.mkdir(parents=True, exist_ok=True)
        return self


def job_paths(jobs_root: Path, job_id: str) -> JobPaths:
    return JobPaths(root=Path(jobs_root) / job_id)
```

`seg_and_mesh/jobs/__init__.py`:

```python
"""잡 오케스트레이션 (스펙 §7, §9, §12).

단일 사용자·로컬. 잡은 폴더 + status.json 하나다. 파이프라인이 io·segment·
labels·mesh를 순서대로 호출한다.
"""

from seg_and_mesh.jobs.layout import JobPaths, job_paths, new_job_id

__all__ = ["JobPaths", "job_paths", "new_job_id"]
```

`pyproject.toml`의 `dependencies`에 추가:

```toml
    "fastapi>=0.115",
    "uvicorn[standard]>=0.30",
    "python-multipart>=0.0.9",
```

그다음 `uv sync`.

- [ ] **Step 5: 통과를 확인한다**

Run: `uv run pytest tests/jobs/test_layout.py -q`
Expected: 전부 PASS.

- [ ] **Step 6: 커밋한다**

```bash
git add pyproject.toml uv.lock seg_and_mesh/jobs/__init__.py seg_and_mesh/jobs/layout.py tests/jobs/__init__.py tests/jobs/test_layout.py
git commit -m "feat(jobs): 웹 의존성과 잡 디렉터리 레이아웃"
```

---

## Task 2: status.json 상태 모델 + PHI-안전 에러

**Files:**
- Create: `seg_and_mesh/jobs/status.py`
- Modify: `seg_and_mesh/jobs/__init__.py`
- Test: `tests/jobs/test_status.py`

**Interfaces:**
- Consumes: `JobPaths` (Task 1)
- Produces:
  - `@dataclass JobStatus` — 필드 `job_id: str`, `case_name: str`, `created_at: str`, `updated_at: str`, `state: str`, `step: str`, `input: dict`, `series: list`, `selected_series: dict | None`, `engine: dict | None`, `variants: list`, `error: dict | None`
  - `STATES = ("awaiting_series", "running", "done", "error")`
  - `write_status(paths: JobPaths, status: JobStatus) -> None` — `updated_at` 갱신 후 원자적으로 쓴다
  - `read_status(paths: JobPaths) -> JobStatus`
  - `record_error(paths: JobPaths, step: str, returncode: int | None, stderr_tail: str) -> None` — PHI-안전 에러 기록. `state="error"`.
  - `sanitize_stderr(text: str) -> str` — 절대경로·파일명 토큰 마스킹

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`tests/jobs/test_status.py`:

```python
"""status.json 상태 모델 + PHI-안전 에러 (스펙 §9.1, §12)."""

from __future__ import annotations

import json

from seg_and_mesh.jobs.layout import job_paths
from seg_and_mesh.jobs.status import (
    JobStatus,
    read_status,
    record_error,
    sanitize_stderr,
    write_status,
)


def _new_status():
    return JobStatus(
        job_id="job1", case_name="case1",
        created_at="2026-07-22T14:30:11Z", updated_at="2026-07-22T14:30:11Z",
        state="awaiting_series", step="io",
        input={"filename": "study.zip", "kind": "dicom-zip", "bytes": 100},
        series=[], selected_series=None, engine=None, variants=[], error=None,
    )


def test_write_then_read_round_trips(tmp_path):
    p = job_paths(tmp_path, "job1").create()
    write_status(p, _new_status())
    s = read_status(p)
    assert s.job_id == "job1"
    assert s.state == "awaiting_series"


def test_write_updates_timestamp(tmp_path):
    p = job_paths(tmp_path, "job1").create()
    s = _new_status()
    write_status(p, s)
    raw = json.loads(p.status_file.read_text(encoding="utf-8"))
    assert "updatedAt" in raw
    assert raw["jobId"] == "job1"
    # camelCase 계약
    assert "caseName" in raw and "selectedSeries" in raw


def test_record_error_sets_state_and_keeps_no_filename(tmp_path):
    """에러에 입력 경로/파일명이 들어가면 PHI 유출(스펙 §12)."""
    p = job_paths(tmp_path, "job1").create()
    write_status(p, _new_status())
    record_error(
        p, step="segment", returncode=1,
        stderr_tail="/work/jobs/job1/nifti/Hong_Gil_Dong_MPRAGE.nii.gz not found",
    )
    s = read_status(p)
    assert s.state == "error"
    assert s.error["step"] == "segment"
    assert s.error["returncode"] == 1
    assert "Hong_Gil_Dong" not in json.dumps(s.error)


def test_sanitize_masks_paths_and_filenames():
    out = sanitize_stderr("failed at /work/jobs/x/nifti/Patient_Name_T1.nii.gz line 3")
    assert "Patient_Name" not in out
    assert "/work/jobs" not in out
    assert "line 3" in out  # 진단 정보는 남긴다
```

- [ ] **Step 2: 실패를 확인한다**

Run: `uv run pytest tests/jobs/test_status.py -q`
Expected: `ImportError`.

- [ ] **Step 3: 구현한다**

`seg_and_mesh/jobs/status.py`:

```python
"""status.json 상태 모델 + PHI-안전 에러 기록 (스펙 §9.1, §12).

UI는 이 파일을 폴링해 진행을 본다. error에는 원본 파일명·경로·DICOM 태그를
절대 넣지 않는다 — 환자 식별자가 새면 PHI 유출이다.
"""

from __future__ import annotations

import json
import os
import re
from dataclasses import asdict, dataclass, field
from datetime import datetime, timezone

from seg_and_mesh.jobs.layout import JobPaths

STATES = ("awaiting_series", "running", "done", "error")

#: 절대경로(POSIX/Windows)와 파일명 토큰을 통째로 마스킹한다.
_PATH_RE = re.compile(r"(/[\w.\-]+)+|([A-Za-z]:\\[\w.\\\-]+)")
_FILE_RE = re.compile(r"\b[\w.\-]+\.(nii|nii\.gz|mgz|zip|dcm)\b", re.IGNORECASE)


def now_iso() -> str:
    return datetime.now(timezone.utc).strftime("%Y-%m-%dT%H:%M:%SZ")


@dataclass
class JobStatus:
    job_id: str
    case_name: str
    created_at: str
    updated_at: str
    state: str
    step: str
    input: dict
    series: list = field(default_factory=list)
    selected_series: dict | None = None
    engine: dict | None = None
    variants: list = field(default_factory=list)
    error: dict | None = None

    def to_json_dict(self) -> dict:
        """camelCase (스펙 §9.1 계약)."""
        return {
            "jobId": self.job_id,
            "caseName": self.case_name,
            "createdAt": self.created_at,
            "updatedAt": self.updated_at,
            "state": self.state,
            "step": self.step,
            "input": self.input,
            "series": self.series,
            "selectedSeries": self.selected_series,
            "engine": self.engine,
            "variants": self.variants,
            "error": self.error,
        }

    @classmethod
    def from_json_dict(cls, d: dict) -> "JobStatus":
        return cls(
            job_id=d["jobId"], case_name=d.get("caseName", ""),
            created_at=d["createdAt"], updated_at=d["updatedAt"],
            state=d["state"], step=d.get("step", ""),
            input=d.get("input", {}), series=d.get("series", []),
            selected_series=d.get("selectedSeries"), engine=d.get("engine"),
            variants=d.get("variants", []), error=d.get("error"),
        )


def sanitize_stderr(text: str) -> str:
    """절대경로·파일명 토큰을 마스킹한다. 나머지 진단 문구는 남긴다."""
    text = _PATH_RE.sub("<path>", text)
    text = _FILE_RE.sub("<file>", text)
    return text.strip()


def write_status(paths: JobPaths, status: JobStatus) -> None:
    """updated_at 갱신 후 원자적으로 쓴다(임시파일 → rename)."""
    status.updated_at = now_iso()
    tmp = paths.status_file.with_suffix(".json.tmp")
    tmp.write_text(
        json.dumps(status.to_json_dict(), indent=2, ensure_ascii=False),
        encoding="utf-8",
    )
    os.replace(tmp, paths.status_file)


def read_status(paths: JobPaths) -> JobStatus:
    return JobStatus.from_json_dict(
        json.loads(paths.status_file.read_text(encoding="utf-8"))
    )


def record_error(paths: JobPaths, step: str, returncode: int | None, stderr_tail: str) -> None:
    """PHI-안전 에러 기록. state=error."""
    status = read_status(paths)
    status.state = "error"
    status.error = {
        "step": step,
        "returncode": returncode,
        "message": sanitize_stderr(stderr_tail),
    }
    write_status(paths, status)
```

`seg_and_mesh/jobs/__init__.py`에 추가:

```python
from seg_and_mesh.jobs.status import (
    STATES,
    JobStatus,
    read_status,
    record_error,
    sanitize_stderr,
    write_status,
)
```

`__all__`에 `"STATES"`, `"JobStatus"`, `"read_status"`, `"record_error"`, `"sanitize_stderr"`, `"write_status"` 추가.

- [ ] **Step 4: 통과를 확인한다**

Run: `uv run pytest tests/jobs/test_status.py -q`
Expected: 전부 PASS.

- [ ] **Step 5: 커밋한다**

```bash
git add seg_and_mesh/jobs/status.py seg_and_mesh/jobs/__init__.py tests/jobs/test_status.py
git commit -m "feat(jobs): status.json 상태 모델과 PHI-안전 에러 기록"
```

---

## Task 3: regions-meta.json 조립기

**Files:**
- Create: `seg_and_mesh/jobs/meta.py`
- Modify: `seg_and_mesh/jobs/__init__.py`
- Test: `tests/jobs/test_meta.py`

**Interfaces:**
- Consumes: `mesh.VariantResult.regions`(라벨별 dict), `labels.CANONICAL_VERSION`, seg/orig NIfTI의 affine·shape
- Produces:
  - `build_regions_meta(seg_path: Path, regions: list[dict], variant_id: str, engine: dict, seg_file: str) -> dict` — 스펙 §2.2 regions-meta.json 전체를 조립한다. `space.toMNI152`는 넣지 않는다(todo).
  - `write_regions_meta(out_path: Path, meta: dict) -> None`

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`tests/jobs/test_meta.py`:

```python
"""regions-meta.json 조립 (스펙 §2.2)."""

from __future__ import annotations

import numpy as np
import nibabel as nib

from seg_and_mesh.jobs.meta import build_regions_meta, write_regions_meta


def _seg(tmp_path):
    vol = np.zeros((4, 4, 4), np.uint8)
    vol[0, 0, 0] = 13
    affine = np.diag([1.0, 1.0, 1.0, 1.0])
    img = nib.Nifti1Image(vol, affine)
    img.header.set_zooms((1.0, 1.0, 1.0))
    p = tmp_path / "seg.nii.gz"
    nib.save(img, p)
    return p


def _regions():
    return [{
        "labelId": 13, "name": "Left-Hippocampus", "color": [220, 216, 20],
        "nodeName": "label_13", "fsId": 17, "group": "subcortical", "side": "L",
        "volumeMm3": 3808.0, "centroid": [-32.6, 3.9, -12.7], "triangleCount": 6364,
    }]


def test_header_and_regions(tmp_path):
    meta = build_regions_meta(
        _seg(tmp_path), _regions(), "v01-3c81",
        engine={"name": "fastsurfer", "version": "2.5.4"},
        seg_file="aparc.DKTatlas+aseg.deep.withCC.mgz",
    )
    assert meta["version"] == 2
    assert meta["labelTable"] == "canonical-v1"
    assert meta["meshVariantId"] == "v01-3c81"
    assert meta["source"]["engine"] == "fastsurfer"
    assert meta["source"]["segFile"] == "aparc.DKTatlas+aseg.deep.withCC.mgz"
    assert meta["regions"] == _regions()


def test_space_has_native_affine_not_mni(tmp_path):
    """space.affine은 native. toMNI152는 지금 안 넣는다(todo)."""
    meta = build_regions_meta(
        _seg(tmp_path), _regions(), "v01-3c81",
        engine={"name": "fastsurfer", "version": "2.5.4"},
        seg_file="seg.mgz",
    )
    assert len(meta["space"]["affine"]) == 4
    assert meta["space"]["shape"] == [4, 4, 4]
    assert meta["space"]["voxelSize"] == [1.0, 1.0, 1.0]
    assert "toMNI152" not in meta["space"]


def test_write_reads_back(tmp_path):
    import json

    meta = build_regions_meta(
        _seg(tmp_path), _regions(), "v01-3c81",
        engine={"name": "fastsurfer", "version": "2.5.4"}, seg_file="seg.mgz",
    )
    out = tmp_path / "regions-meta.json"
    write_regions_meta(out, meta)
    assert json.loads(out.read_text(encoding="utf-8"))["version"] == 2
```

- [ ] **Step 2: 실패를 확인한다**

Run: `uv run pytest tests/jobs/test_meta.py -q`
Expected: `ImportError`.

- [ ] **Step 3: 구현한다**

`seg_and_mesh/jobs/meta.py`:

```python
"""regions-meta.json 조립 (스펙 §2.2).

mesh 엔진은 라벨별 regions 배열만 낸다. 여기서 헤더(space·source·labelTable)를
붙여 소비 측(brain-educate) 계약을 완성한다. space.toMNI152는 지금 넣지 않는다
— brain-educate 뇌간 임시해가 있고, SimpleITK MNI152 등록은 별도 작업이다.
"""

from __future__ import annotations

import json
from pathlib import Path

import nibabel as nib
import numpy as np

from seg_and_mesh.labels import CANONICAL_VERSION


def build_regions_meta(seg_path, regions, variant_id, engine, seg_file) -> dict:
    """스펙 §2.2 regions-meta.json 전체."""
    img = nib.load(Path(seg_path))
    affine = np.asarray(img.affine, dtype=float)
    zooms = [float(z) for z in img.header.get_zooms()[:3]]
    return {
        "version": 2,
        "labelTable": CANONICAL_VERSION,
        "space": {
            "affine": affine.tolist(),
            "shape": [int(s) for s in img.shape[:3]],
            "voxelSize": zooms,
        },
        "source": {
            "engine": engine.get("name"),
            "engineVersion": engine.get("version"),
            "segFile": seg_file,
        },
        "meshConfig": None,  # 탐색 단계 — 정본 확정 전(todo §6.6)
        "meshVariantId": variant_id,
        "regions": regions,
    }


def write_regions_meta(out_path, meta: dict) -> None:
    Path(out_path).write_text(
        json.dumps(meta, indent=2, ensure_ascii=False), encoding="utf-8"
    )
```

`seg_and_mesh/jobs/__init__.py`에 추가:

```python
from seg_and_mesh.jobs.meta import build_regions_meta, write_regions_meta
```

`__all__`에 `"build_regions_meta"`, `"write_regions_meta"` 추가.

- [ ] **Step 4: 통과를 확인한다**

Run: `uv run pytest tests/jobs/test_meta.py -q`
Expected: 전부 PASS.

- [ ] **Step 5: 커밋한다**

```bash
git add seg_and_mesh/jobs/meta.py seg_and_mesh/jobs/__init__.py tests/jobs/test_meta.py
git commit -m "feat(jobs): regions-meta.json 조립기(space native affine, toMNI152 제외)"
```

---

## Task 4: 파이프라인 오케스트레이션 + 백그라운드 러너

**Files:**
- Create: `seg_and_mesh/jobs/pipeline.py`
- Modify: `seg_and_mesh/jobs/__init__.py`
- Modify: `seg_and_mesh/segment/fastsurfer.py` (호스트 마운트 경로 인자 추가 — 아래)
- Test: `tests/jobs/test_pipeline.py`, `tests/segment/test_fastsurfer.py`(추가)

**Interfaces:**
- Consumes: io(`prepare_input`·`run_dcm2niix`·`describe_nifti`·`rank_series`), `segment.run_fastsurfer`, `labels.remap_segmentation`, `mesh.generate_variant`, Task 1~3(`to_host_path` 포함)
- Produces:
  - `ingest_job(paths: JobPaths, src: Path, filename: str, *, dcm2niix_runner=None) -> JobStatus` — io 단계. 시리즈 랭킹 후 `state="awaiting_series"`로 멈춘다(스펙 §6.3 게이트).
  - `run_segmentation_and_mesh(paths: JobPaths, selected_nifti: Path, image: str, *, threads: int = 8, fastsurfer_runner=None, table=None, jobs_root: Path | None = None, host_jobs_root: Path | None = None) -> JobStatus` — 선택 후: segment→remap→mesh(기본 변형)→4파일 산출. 단계마다 status 갱신, 실패 시 `record_error`.
  - `SegmentConfig` 는 만들지 않는다 — image·threads를 인자로 받는다.

**`segment.build_fastsurfer_command` / `run_fastsurfer` 수정 (형제 컨테이너 경로):**

현재 두 함수는 `-v` 마운트 원본으로 자기가 받은 경로를 그대로 쓴다. api가 컨테이너 안에서 돌면 그 경로는 도커 데몬(호스트)이 못 찾는다. 그래서 **마운트에 쓸 호스트 경로**를 로컬 파일 접근 경로와 분리한다. 키워드 전용 인자 둘을 추가하고, `None`이면 지금 동작 그대로다(하위호환).

```python
def build_fastsurfer_command(
    t1_path, subject_dir_root, sid, image, threads=8,
    *, host_t1_dir=None, host_subject_dir_root=None,
):
    """...

    host_t1_dir / host_subject_dir_root: `-v` 마운트 원본으로 쓸 호스트 경로.
        api가 컨테이너 안에서 돌 때 필요하다 — 도커 데몬은 호스트에 있어
        api 내부 경로를 모른다. None이면 t1_path.parent / subject_dir_root를
        그대로 쓴다(호스트 직접 실행).
    """
    t1_path = Path(t1_path)
    mount_t1_dir = Path(host_t1_dir) if host_t1_dir else t1_path.parent
    mount_sd = Path(host_subject_dir_root) if host_subject_dir_root else Path(subject_dir_root)
    return [
        "docker", "run", "--rm", "--gpus", "all", "--user", "root",
        "-v", f"{mount_t1_dir}:/data:ro",
        "-v", f"{mount_sd}:/output",
        ...  # 나머지 인자는 그대로
    ]
```

`run_fastsurfer`도 같은 두 키워드를 받아 `build_fastsurfer_command`로 넘긴다. **파일을 읽고 쓰는 코드(`subject_dir_root / sid / "mri"`, `_write_orig_nifti`)는 절대 호스트 경로를 쓰면 안 된다** — 그건 api 자기 파일시스템 경로여야 한다. 마운트 인자에만 쓴다.

`tests/segment/test_fastsurfer.py`에 추가할 테스트:
- 두 키워드가 `None`이면 명령이 지금과 동일(기존 테스트가 이미 커버 — 회귀 확인).
- `host_subject_dir_root=Path("/host/out")`를 주면 `-v /host/out:/output`이 나오고, **`--sd`는 여전히 `/output`**, 그리고 결과 파일은 로컬 `subject_dir_root` 밑에서 찾는다.

`run_segmentation_and_mesh`는 `to_host_path(paths.fs_dir, ...)`와 `to_host_path(selected_nifti.parent, ...)`로 두 값을 만들어 넘긴다. `jobs_root`가 `None`이면 변환 없이(둘 다 `None`) 부른다.

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`tests/jobs/test_pipeline.py`:

```python
"""파이프라인 오케스트레이션 (스펙 §6, §12, §13 — FastSurfer 목)."""

from __future__ import annotations

from pathlib import Path
from types import SimpleNamespace

import nibabel as nib
import numpy as np

from seg_and_mesh.jobs.layout import job_paths
from seg_and_mesh.jobs.pipeline import ingest_job, run_segmentation_and_mesh
from seg_and_mesh.jobs.status import read_status
from seg_and_mesh.segment import SEG_SOURCE_FILE


def _nifti(path, value_map=None, shape=(16, 16, 16), zooms=(1.0, 1.0, 1.0)):
    vol = np.zeros(shape, np.int16)
    for (i, j, k), v in (value_map or {}).items():
        vol[i, j, k] = v
    img = nib.Nifti1Image(vol, np.diag([*zooms, 1.0]))
    img.header.set_zooms(zooms)
    path.parent.mkdir(parents=True, exist_ok=True)
    nib.save(img, path)
    return path


def _t1_upload(tmp_path):
    """직접 NIfTI 업로드를 흉내낸다(dcm2niix 없이)."""
    return _nifti(tmp_path / "up" / "input.nii.gz", shape=(16, 16, 16))


def test_ingest_nifti_stops_at_awaiting_series(tmp_path):
    p = job_paths(tmp_path / "jobs", "job1").create()
    src = _t1_upload(tmp_path)
    status = ingest_job(p, src, "input.nii.gz")
    assert status.state == "awaiting_series"
    assert len(status.series) == 1  # 직접 NIfTI는 시리즈 하나
    # 게이트: 아직 segment 안 돌았다
    assert not (p.seg_dir / "seg.nii.gz").exists()


def _fastsurfer_mock(subject_dir_root, sid):
    """FastSurfer 성공을 흉내낸다 — orig.mgz + withCC 세그를 쓴다."""
    def run(cmd, **kwargs):
        mri = Path(subject_dir_root) / sid / "mri"
        mri.mkdir(parents=True, exist_ok=True)
        nib.save(nib.MGHImage(np.full((16, 16, 16), 100, np.uint8), np.eye(4)),
                 mri / "orig.mgz")
        seg = np.zeros((16, 16, 16), np.int16)
        seg[2:12, 2:12, 2:12] = 17  # 해마 덩어리(min_voxel 넘게)
        nib.save(nib.MGHImage(seg, np.eye(4)), mri / SEG_SOURCE_FILE)
        return SimpleNamespace(returncode=0, stdout="ok", stderr="")
    return run


def test_full_run_produces_four_files(tmp_path):
    p = job_paths(tmp_path / "jobs", "job1").create()
    src = _t1_upload(tmp_path)
    ingest_job(p, src, "input.nii.gz")
    selected = read_status(p).series[0]["niftiPath"]

    status = run_segmentation_and_mesh(
        p, Path(selected), image="fs:tag",
        fastsurfer_runner=_fastsurfer_mock(p.fs_dir, "case"),
    )

    assert status.state == "done"
    assert (p.seg_dir / "seg.nii.gz").is_file()
    assert (p.seg_dir / "orig.nii.gz").is_file()
    # 변형 하나
    assert len(status.variants) == 1
    vid = status.variants[0]["variantId"]
    vdir = p.variant_dir(vid)
    assert (vdir / "regions.glb").is_file()
    assert (vdir / "regions-meta.json").is_file()
    assert (vdir / "metrics.json").is_file()
    assert (vdir / "params.json").is_file()


def test_segment_failure_records_phi_safe_error(tmp_path):
    p = job_paths(tmp_path / "jobs", "job1").create()
    src = _t1_upload(tmp_path)
    ingest_job(p, src, "input.nii.gz")
    selected = read_status(p).series[0]["niftiPath"]

    def failing(cmd, **kwargs):
        return SimpleNamespace(
            returncode=1, stdout="",
            stderr=f"error at {selected}",  # 경로가 stderr에 있다
        )

    status = run_segmentation_and_mesh(
        p, Path(selected), image="fs:tag", fastsurfer_runner=failing,
    )
    assert status.state == "error"
    assert status.error["step"] == "segment"
    import json
    assert ".nii.gz" not in json.dumps(status.error) or "<file>" in json.dumps(status.error)
```

- [ ] **Step 2: 실패를 확인한다**

Run: `uv run pytest tests/jobs/test_pipeline.py -q`
Expected: `ImportError`.

- [ ] **Step 3: 구현한다**

`seg_and_mesh/jobs/pipeline.py`:

```python
"""파이프라인 오케스트레이션 (스펙 §6, §12).

io → (게이트) → segment → remap → mesh를 순서대로 호출하고 status.json을
갱신한다. FastSurfer 실패 등은 record_error로 PHI-안전하게 남긴다.

시리즈 선택은 게이트다(스펙 §6.3). ingest_job은 랭킹까지만 하고 멈춘다.
사용자가 고르면 run_segmentation_and_mesh가 이어 돈다.
"""

from __future__ import annotations

import shutil
from pathlib import Path

import nibabel as nib

from seg_and_mesh.io import (
    describe_nifti,
    prepare_input,
    rank_series,
    run_dcm2niix,
)
from seg_and_mesh.jobs.layout import JobPaths, to_host_path
from seg_and_mesh.jobs.meta import build_regions_meta, write_regions_meta
from seg_and_mesh.jobs.status import (
    JobStatus,
    now_iso,
    read_status,
    record_error,
    write_status,
)
from seg_and_mesh.labels import load_canonical, remap_segmentation
from seg_and_mesh.mesh import default_params, generate_variant
from seg_and_mesh.segment import SEG_SOURCE_FILE, SegmentError, run_fastsurfer


def _series_dict(candidate) -> dict:
    s = candidate.series
    return {
        "niftiPath": str(s.nifti_path),
        "description": s.series_description,
        "slices": s.slices,
        "voxelSizeMm": list(s.voxel_size_mm),
        "acquisitionType": s.acquisition_type,
        "score": candidate.score,
        "reasons": candidate.reasons,
    }


def ingest_job(paths: JobPaths, src: Path, filename: str, *, dcm2niix_runner=None) -> JobStatus:
    """io 단계. 랭킹 후 awaiting_series로 멈춘다(스펙 §6.3 게이트)."""
    prepared = prepare_input(Path(src), paths.nifti_dir)

    if prepared.nifti_file is not None:
        series = [describe_nifti(prepared.nifti_file, None)]
    else:
        series = run_dcm2niix(prepared.dicom_dir, paths.nifti_dir)

    ranked = rank_series(series)

    status = JobStatus(
        job_id=paths.root.name,
        case_name=paths.root.name,
        created_at=now_iso(),
        updated_at=now_iso(),
        state="awaiting_series",
        step="io",
        input={"filename": filename, "bytes": Path(src).stat().st_size},
        series=[_series_dict(c) for c in ranked],
    )
    write_status(paths, status)
    return status


def run_segmentation_and_mesh(
    paths: JobPaths,
    selected_nifti: Path,
    image: str,
    *,
    threads: int = 8,
    fastsurfer_runner=None,
    table=None,
    jobs_root: Path | None = None,
    host_jobs_root: Path | None = None,
) -> JobStatus:
    """선택 후: segment → remap → mesh(기본 변형) → 4파일. 실패는 PHI-안전 기록.

    jobs_root/host_jobs_root: api가 컨테이너 안에서 돌 때, 형제 FastSurfer
        컨테이너의 `-v` 인자를 호스트 경로로 바꾸기 위한 짝(Task 1
        to_host_path). jobs_root가 None이면 변환하지 않는다.
    """
    table = table or load_canonical()
    status = read_status(paths)
    status.state = "running"
    status.selected_series = {"niftiPath": str(selected_nifti)}
    status.step = "segment"
    write_status(paths, status)

    # --- segment ---
    try:
        kwargs = {"sid": "case", "threads": threads}
        if fastsurfer_runner is not None:
            kwargs["runner"] = fastsurfer_runner
        if jobs_root is not None:
            # 형제 컨테이너 -v 인자는 호스트 경로여야 한다(Task 1 to_host_path).
            kwargs["host_t1_dir"] = to_host_path(
                selected_nifti.parent, jobs_root=jobs_root, host_jobs_root=host_jobs_root
            )
            kwargs["host_subject_dir_root"] = to_host_path(
                paths.fs_dir, jobs_root=jobs_root, host_jobs_root=host_jobs_root
            )
        seg_result = run_fastsurfer(selected_nifti, paths.fs_dir, image, **kwargs)
    except SegmentError as exc:
        record_error(paths, "segment", None, str(exc))
        return read_status(paths)

    # orig을 seg/로 옮긴다
    orig_dst = paths.seg_dir / "orig.nii.gz"
    shutil.copy2(seg_result.orig_path, orig_dst)

    # --- remap ---
    status = read_status(paths)
    status.step = "remap"
    status.engine = {"name": "fastsurfer", "version": _image_version(image), "device": "cuda"}
    write_status(paths, status)

    seg_canon = paths.seg_dir / "seg.nii.gz"
    remap_segmentation(seg_result.seg_source_path, seg_canon, table)

    # --- mesh (기본 변형 하나) ---
    status = read_status(paths)
    status.step = "mesh"
    write_status(paths, status)

    params = default_params()
    variant = generate_variant(seg_canon, paths.mesh_dir / "tmp", params, index=1, table=table)
    vdir = paths.variant_dir(variant.variant_id)
    # generate_variant는 out_dir에 직접 쓴다 — 올바른 variant_dir로 다시 만든다
    variant = generate_variant(seg_canon, vdir, params, index=1, table=table)

    meta = build_regions_meta(
        seg_canon, variant.regions, variant.variant_id,
        engine=status.engine, seg_file=SEG_SOURCE_FILE,
    )
    write_regions_meta(vdir / "regions-meta.json", meta)

    # --- 완료 ---
    status = read_status(paths)
    status.state = "done"
    status.step = "done"
    status.variants = [{
        "variantId": variant.variant_id,
        "bytes": variant.metrics["glbBytes"],
        "createdAt": variant.params["createdAt"],
    }]
    write_status(paths, status)
    return read_status(paths)


def _image_version(image: str) -> str:
    """이미지 태그에서 버전 문자열을 뽑는다(예: deepmi/fastsurfer:cuda-v2.5.4 → cuda-v2.5.4)."""
    return image.rsplit(":", 1)[-1] if ":" in image else image
```

**주의(구현자):** `generate_variant`를 두 번 부르는 위 코드는 낭비다. `mesh/tmp`로 먼저 부르는 줄을 지우고 `vdir`로 한 번만 부른다 — 위 초안의 첫 호출은 지운다. 최종 코드:

```python
    params = default_params()
    variant = generate_variant(seg_canon, paths.variant_dir("tmp"), params, index=1, table=table)
    vdir = paths.variant_dir(variant.variant_id)
    if vdir.exists():
        shutil.rmtree(vdir)
    (paths.variant_dir("tmp")).rename(vdir)
```

variantId를 파라미터 해시로 먼저 계산할 수 있으므로(스펙 §7), 더 깔끔한 방법은:

```python
    params = default_params()
    variant_id = params.variant_id(1)
    vdir = paths.variant_dir(variant_id)
    variant = generate_variant(seg_canon, vdir, params, index=1, table=table)
```

**이 세 번째 형태를 쓴다** — `variant_id`를 먼저 계산해 바로 올바른 폴더에 생성한다. 위 두 초안은 참고용이고, 구현은 이 형태다.

`seg_and_mesh/jobs/__init__.py`에 추가:

```python
from seg_and_mesh.jobs.pipeline import ingest_job, run_segmentation_and_mesh
```

`__all__`에 `"ingest_job"`, `"run_segmentation_and_mesh"` 추가.

- [ ] **Step 4: 통과를 확인한다**

Run: `uv run pytest tests/jobs/test_pipeline.py -q`
Expected: 전부 PASS.

- [ ] **Step 5: 전체 회귀**

Run: `uv run pytest -W error -q`
Expected: 실패 0.

- [ ] **Step 6: 커밋한다**

```bash
git add seg_and_mesh/jobs/pipeline.py seg_and_mesh/jobs/__init__.py tests/jobs/test_pipeline.py
git commit -m "feat(jobs): io→segment→remap→mesh 파이프라인과 시리즈 게이트"
```

---

## Task 5: FastAPI 라우트

**Files:**
- Create: `seg_and_mesh/web/__init__.py`, `seg_and_mesh/web/app.py`
- Test: `tests/web/__init__.py`, `tests/web/test_app.py`

**Interfaces:**
- Consumes: Task 1~4, 환경변수(`OUTPUT_DIR`·`FASTSURFER_IMAGE` 등)
- Produces:
  - `create_app(config: AppConfig) -> FastAPI`
  - `@dataclass AppConfig` — `jobs_root: Path`, `fastsurfer_image: str`, `threads: int`, `fastsurfer_runner=None`(테스트 주입)
  - 라우트:
    - `POST /api/jobs` (multipart 파일) → `{jobId}`; io 실행 후 awaiting_series
    - `GET /api/jobs/{jobId}` → status.json
    - `POST /api/jobs/{jobId}/series` `{niftiPath}` → 백그라운드로 segment+mesh 시작, `{state:"running"}`
    - `GET /api/jobs/{jobId}/variants/{variantId}/regions.glb` → GLB 파일
    - `GET /api/jobs/{jobId}/variants/{variantId}/regions-meta.json` → 메타
    - `GET /` → 뷰어 index.html

- [ ] **Step 1: `tests/web/__init__.py`를 빈 파일로 만든다**

- [ ] **Step 2: 실패하는 테스트를 쓴다**

`tests/web/test_app.py`:

```python
"""FastAPI 라우트 (스펙 §4, §6.3)."""

from __future__ import annotations

import io
import time
from pathlib import Path
from types import SimpleNamespace

import nibabel as nib
import numpy as np
from fastapi.testclient import TestClient

from seg_and_mesh.segment import SEG_SOURCE_FILE
from seg_and_mesh.web.app import AppConfig, create_app


def _nifti_bytes():
    import tempfile
    vol = np.zeros((16, 16, 16), np.int16)
    img = nib.Nifti1Image(vol, np.eye(4))
    img.header.set_zooms((1.0, 1.0, 1.0))
    with tempfile.TemporaryDirectory() as d:
        p = Path(d) / "input.nii.gz"
        nib.save(img, p)
        return p.read_bytes()


def _fastsurfer_mock(root_getter):
    def run(cmd, **kwargs):
        # --sd 다음 인자가 subject_dir_root, --sid 다음이 sid
        sd = Path(cmd[cmd.index("--sd") + 1])
        # 컨테이너 내부 경로가 아니라 호스트 경로를 목이 알아야 하므로
        # root_getter로 실제 fs_dir를 받는다
        mri = root_getter() / "case" / "mri"
        mri.mkdir(parents=True, exist_ok=True)
        nib.save(nib.MGHImage(np.full((16, 16, 16), 100, np.uint8), np.eye(4)), mri / "orig.mgz")
        seg = np.zeros((16, 16, 16), np.int16)
        seg[2:12, 2:12, 2:12] = 17
        nib.save(nib.MGHImage(seg, np.eye(4)), mri / SEG_SOURCE_FILE)
        return SimpleNamespace(returncode=0, stdout="ok", stderr="")
    return run


def _client(tmp_path):
    holder = {}
    cfg = AppConfig(
        jobs_root=tmp_path / "jobs",
        fastsurfer_image="fs:tag",
        threads=4,
        fastsurfer_runner=_fastsurfer_mock(lambda: holder["fs_dir"]),
    )
    app = create_app(cfg)
    return TestClient(app), holder


def test_upload_creates_job_awaiting_series(tmp_path):
    client, _ = _client(tmp_path)
    r = client.post("/api/jobs", files={"file": ("input.nii.gz", _nifti_bytes())})
    assert r.status_code == 200
    jid = r.json()["jobId"]

    s = client.get(f"/api/jobs/{jid}").json()
    assert s["state"] == "awaiting_series"
    assert len(s["series"]) == 1


def test_series_selection_runs_pipeline_to_done(tmp_path):
    client, holder = _client(tmp_path)
    jid = client.post("/api/jobs", files={"file": ("input.nii.gz", _nifti_bytes())}).json()["jobId"]
    holder["fs_dir"] = tmp_path / "jobs" / jid / "fs"

    nifti_path = client.get(f"/api/jobs/{jid}").json()["series"][0]["niftiPath"]
    r = client.post(f"/api/jobs/{jid}/series", json={"niftiPath": nifti_path})
    assert r.status_code == 200

    # 백그라운드 완료 대기 (테스트는 동기 실행하거나 폴링)
    for _ in range(50):
        s = client.get(f"/api/jobs/{jid}").json()
        if s["state"] in ("done", "error"):
            break
        time.sleep(0.1)
    assert s["state"] == "done", s.get("error")
    assert len(s["variants"]) == 1


def test_index_serves_viewer(tmp_path):
    client, _ = _client(tmp_path)
    r = client.get("/")
    assert r.status_code == 200
    assert "text/html" in r.headers["content-type"]
```

- [ ] **Step 3: 실패를 확인한다**

Run: `uv run pytest tests/web/test_app.py -q`
Expected: `ImportError`.

- [ ] **Step 4: 구현한다**

`seg_and_mesh/web/app.py`:

```python
"""FastAPI 워크벤치 (스펙 §4, §6.3).

단일 사용자·로컬. 업로드 → 시리즈 게이트 → 백그라운드 파이프라인 → 뷰어.
"""

from __future__ import annotations

import threading
from dataclasses import dataclass
from pathlib import Path

from fastapi import BackgroundTasks, FastAPI, HTTPException, UploadFile
from fastapi.responses import FileResponse, HTMLResponse
from pydantic import BaseModel

from seg_and_mesh.jobs.layout import job_paths, new_job_id
from seg_and_mesh.jobs.pipeline import ingest_job, run_segmentation_and_mesh
from seg_and_mesh.jobs.status import read_status

_STATIC = Path(__file__).resolve().parent / "static"


@dataclass
class AppConfig:
    jobs_root: Path
    fastsurfer_image: str
    threads: int = 8
    fastsurfer_runner=None
    #: 컨테이너 안에서 돌 때, jobs_root가 호스트에서 어디에 있는지.
    #: 형제 FastSurfer 컨테이너의 -v 인자에 쓴다. 호스트 직접 실행이면 None.
    host_jobs_root: Path | None = None


class SeriesSelection(BaseModel):
    niftiPath: str


def create_app(config: AppConfig) -> FastAPI:
    app = FastAPI(title="seg-and-mesh")
    config.jobs_root.mkdir(parents=True, exist_ok=True)

    @app.post("/api/jobs")
    async def upload(file: UploadFile) -> dict:
        job_id = new_job_id()
        paths = job_paths(config.jobs_root, job_id).create()
        dst = paths.input_dir / (file.filename or "upload")
        dst.write_bytes(await file.read())
        try:
            ingest_job(paths, dst, file.filename or "upload")
        except Exception as exc:  # io 실패도 PHI 없이
            from seg_and_mesh.jobs.status import sanitize_stderr
            raise HTTPException(400, sanitize_stderr(str(exc)))
        return {"jobId": job_id}

    @app.get("/api/jobs/{job_id}")
    def status(job_id: str) -> dict:
        paths = job_paths(config.jobs_root, job_id)
        if not paths.status_file.is_file():
            raise HTTPException(404, "job 없음")
        return read_status(paths).to_json_dict()

    @app.post("/api/jobs/{job_id}/series")
    def select_series(job_id: str, sel: SeriesSelection, bg: BackgroundTasks) -> dict:
        paths = job_paths(config.jobs_root, job_id)
        if not paths.status_file.is_file():
            raise HTTPException(404, "job 없음")

        def work():
            run_segmentation_and_mesh(
                paths, Path(sel.niftiPath), config.fastsurfer_image,
                threads=config.threads, fastsurfer_runner=config.fastsurfer_runner,
                jobs_root=config.jobs_root, host_jobs_root=config.host_jobs_root,
            )

        bg.add_task(work)
        return {"state": "running"}

    @app.get("/api/jobs/{job_id}/variants/{variant_id}/regions.glb")
    def glb(job_id: str, variant_id: str) -> FileResponse:
        p = job_paths(config.jobs_root, job_id).variant_dir(variant_id) / "regions.glb"
        if not p.is_file():
            raise HTTPException(404, "glb 없음")
        return FileResponse(p, media_type="model/gltf-binary")

    @app.get("/api/jobs/{job_id}/variants/{variant_id}/regions-meta.json")
    def meta(job_id: str, variant_id: str) -> FileResponse:
        p = job_paths(config.jobs_root, job_id).variant_dir(variant_id) / "regions-meta.json"
        if not p.is_file():
            raise HTTPException(404, "meta 없음")
        return FileResponse(p, media_type="application/json")

    @app.get("/", response_class=HTMLResponse)
    def index() -> str:
        return (_STATIC / "index.html").read_text(encoding="utf-8")

    return app
```

`seg_and_mesh/web/__init__.py`:

```python
"""FastAPI 워크벤치 (스펙 §4, §8)."""

from seg_and_mesh.web.app import AppConfig, create_app

__all__ = ["AppConfig", "create_app"]
```

**주의:** `static/index.html`이 없으면 `test_index_serves_viewer`가 깨진다. Task 6이 만들지만, 이 태스크 통과를 위해 최소 플레이스홀더 `static/index.html`("<!doctype html><title>seg-and-mesh</title>")을 먼저 두고 Task 6에서 채운다.

`FastFastSurfer runner` 주입: `AppConfig.fastsurfer_runner`가 None이면 `run_fastsurfer`가 기본 `subprocess.run`을 쓴다(실제 docker). 테스트는 목을 주입한다.

**BackgroundTasks 주의:** FastAPI `TestClient`는 `BackgroundTasks`를 응답 반환 **후 동기로** 실행한다. 따라서 테스트의 폴링 루프는 사실상 즉시 done을 본다. 실서버(uvicorn)에서는 실제 백그라운드다. 한 번에 잡 하나 전제이므로 스레드 경합은 없다.

- [ ] **Step 5: 최소 플레이스홀더 뷰어를 둔다**

`seg_and_mesh/web/static/index.html`:

```html
<!doctype html>
<title>seg-and-mesh</title>
<p>viewer placeholder — Task 6</p>
```

- [ ] **Step 6: 통과를 확인한다**

Run: `uv run pytest tests/web/test_app.py -q`
Expected: 전부 PASS.

- [ ] **Step 7: 커밋한다**

```bash
git add seg_and_mesh/web tests/web
git commit -m "feat(web): FastAPI 라우트(업로드·시리즈 게이트·백그라운드 실행·파일 서빙)"
```

---

## Task 6: three.js 뷰어

**Files:**
- Modify: `seg_and_mesh/web/static/index.html`
- Create: `seg_and_mesh/web/static/viewer.js`
- Create: `seg_and_mesh/web/static/vendor/` (three.js 파일들 — 아래 Step 0)
- Test: 수동(뷰어 JS는 자동 단위테스트가 어렵다). API 스모크는 Task 5가 커버.

**Interfaces:**
- Consumes: `GET /api/jobs/{id}/variants/{vid}/regions.glb`, `.../regions-meta.json`
- Produces: 단일 변형 뷰어 — 불투명/반투명 토글, group/side 토글, 지표 오버레이. 색은 regions-meta.json에서 칠한다(B안).

**설계:** three.js는 **저장소에 vendor해 커밋한다.** 런타임에 인터넷을 타지 않는다 — 환자 데이터를 다루는 로컬 단독 툴이라 외부 CDN 요청을 안 만드는 게 맞고, 버전이 고정되어 재현성도 확보된다. importmap은 그대로 쓰되 CDN URL 대신 `/static/vendor/`를 가리킨다.

`.gitignore`의 `/vendor/`는 **루트 고정 패턴**이라 `seg_and_mesh/web/static/vendor/`는 무시되지 않는다. 확인: `git check-ignore -v seg_and_mesh/web/static/vendor/three.module.js` 가 아무것도 출력하지 않아야 한다.

- [ ] **Step 0: three.js r0.160.0을 vendor한다**

```powershell
$V = "0.160.0"
$D = "seg_and_mesh/web/static/vendor"
New-Item -ItemType Directory -Force "$D/addons/loaders","$D/addons/controls","$D/addons/utils" | Out-Null
Invoke-WebRequest "https://unpkg.com/three@$V/build/three.module.js" -OutFile "$D/three.module.js"
Invoke-WebRequest "https://unpkg.com/three@$V/examples/jsm/loaders/GLTFLoader.js" -OutFile "$D/addons/loaders/GLTFLoader.js"
Invoke-WebRequest "https://unpkg.com/three@$V/examples/jsm/controls/OrbitControls.js" -OutFile "$D/addons/controls/OrbitControls.js"
Invoke-WebRequest "https://unpkg.com/three@$V/examples/jsm/utils/BufferGeometryUtils.js" -OutFile "$D/addons/utils/BufferGeometryUtils.js"
```

`GLTFLoader.js`는 `'three'` 말고 **상대경로로 다른 addon을 import한다**(r160에선 `../utils/BufferGeometryUtils.js`). 받은 파일 셋에서 남은 상대 import가 없는지 닫힐 때까지 확인한다:

```powershell
Select-String -Path "$D/addons/**/*.js" -Pattern "from '\.\./" 
```

나오는 경로가 전부 받은 파일에 있어야 한다. 없으면 같은 방식으로 더 받는다.

`$D/README.md`에 출처와 버전을 남긴다:

```markdown
# three.js (vendored)

three.js r0.160.0 — https://unpkg.com/three@0.160.0/

런타임에 CDN을 타지 않으려고 저장소에 넣었다(로컬 단독 툴, 환자 데이터).
갱신하려면 계획 문서 Task 6 Step 0의 스크립트에서 버전만 바꿔 다시 받는다.
MIT License (three.js).
```

- [ ] **Step 1: index.html을 쓴다**

`seg_and_mesh/web/static/index.html`:

```html
<!doctype html>
<html lang="ko">
<head>
<meta charset="utf-8">
<title>seg-and-mesh 뷰어</title>
<style>
  body { margin: 0; font-family: system-ui, sans-serif; background: #111; color: #eee; }
  #canvas { position: fixed; inset: 0; }
  #panel { position: fixed; top: 10px; left: 10px; background: rgba(0,0,0,.6);
           padding: 12px; border-radius: 8px; max-width: 320px; font-size: 13px; }
  #panel input[type=text] { width: 180px; }
  button { margin: 2px; }
  #metrics { white-space: pre; font-family: monospace; font-size: 11px; margin-top: 8px; }
  label { display: block; }
</style>
<script type="importmap">
{ "imports": {
  "three": "/static/vendor/three.module.js",
  "three/addons/": "/static/vendor/addons/"
} }
</script>
</head>
<body>
<canvas id="canvas"></canvas>
<div id="panel">
  <div>
    <input id="jobId" type="text" placeholder="jobId">
    <input id="variantId" type="text" placeholder="variantId">
    <button id="load">로드</button>
  </div>
  <div>
    <label><input type="checkbox" id="transparent"> 반투명 셸</label>
  </div>
  <div id="groups"></div>
  <div id="metrics"></div>
</div>
<script type="module" src="/static/viewer.js"></script>
</body>
</html>
```

- [ ] **Step 2: viewer.js를 쓴다**

`seg_and_mesh/web/static/viewer.js`:

```javascript
import * as THREE from 'three';
import { GLTFLoader } from 'three/addons/loaders/GLTFLoader.js';
import { OrbitControls } from 'three/addons/controls/OrbitControls.js';

const canvas = document.getElementById('canvas');
const renderer = new THREE.WebGLRenderer({ canvas, antialias: true, alpha: true });
renderer.setPixelRatio(devicePixelRatio);

const scene = new THREE.Scene();
const camera = new THREE.PerspectiveCamera(35, innerWidth / innerHeight, 0.01, 1000);
const controls = new OrbitControls(camera, canvas);

scene.add(new THREE.AmbientLight(0xffffff, 0.6));
const d1 = new THREE.DirectionalLight(0xffffff, 1.2); d1.position.set(2, 3, 4); scene.add(d1);
const d2 = new THREE.DirectionalLight(0x7799cc, 0.5); d2.position.set(-3, -1, -2); scene.add(d2);

function resize() {
  renderer.setSize(innerWidth, innerHeight, false);
  camera.aspect = innerWidth / innerHeight;
  camera.updateProjectionMatrix();
}
addEventListener('resize', resize); resize();

let root = null;          // 현재 GLB 그룹
let meta = null;          // regions-meta.json
const matById = new Map();

function clear() {
  if (root) { scene.remove(root); root = null; }
  matById.clear();
}

function frame(group) {
  const box = new THREE.Box3().setFromObject(group);
  const center = box.getCenter(new THREE.Vector3());
  const size = box.getSize(new THREE.Vector3());
  const radius = Math.max(size.x, size.y, size.z);
  group.position.sub(center);           // bbox 중심을 원점으로 (스펙 §8)
  camera.position.set(0, 0, radius * 2.2);
  camera.near = radius / 100; camera.far = radius * 10;
  camera.updateProjectionMatrix();
  controls.target.set(0, 0, 0); controls.update();
}

function applyColors(transparent) {
  const byNode = new Map(meta.regions.map(r => [r.nodeName, r]));
  root.traverse(obj => {
    if (!obj.isMesh) return;
    const region = byNode.get(obj.name);
    const c = region ? region.color : [200, 200, 200];
    const mat = new THREE.MeshStandardMaterial({
      color: new THREE.Color(c[0] / 255, c[1] / 255, c[2] / 255),
      side: THREE.DoubleSide,
      transparent: transparent,
      opacity: transparent ? 0.05 : 1.0,
      depthWrite: !transparent,
    });
    obj.material = mat;
    if (region) matById.set(region.labelId, obj);
  });
}

async function load(jobId, variantId) {
  clear();
  const base = `/api/jobs/${jobId}/variants/${variantId}`;
  meta = await (await fetch(`${base}/regions-meta.json`)).json();
  const buf = await (await fetch(`${base}/regions.glb`)).arrayBuffer();
  const gltf = await new GLTFLoader().parseAsync(buf, '');
  root = gltf.scene;
  scene.add(root);
  applyColors(document.getElementById('transparent').checked);
  frame(root);
  renderMetrics();
  renderGroups();
}

function renderMetrics() {
  const n = meta.regions.length;
  const vols = meta.regions.map(r => r.volumeMm3);
  document.getElementById('metrics').textContent =
    `변형 ${meta.meshVariantId}\n영역 ${n}개\n라벨표 ${meta.labelTable}`;
}

function renderGroups() {
  const groups = [...new Set(meta.regions.map(r => r.group))];
  const el = document.getElementById('groups');
  el.innerHTML = '';
  for (const g of groups) {
    const lab = document.createElement('label');
    const cb = document.createElement('input');
    cb.type = 'checkbox'; cb.checked = true;
    cb.onchange = () => {
      for (const r of meta.regions) {
        if (r.group === g) {
          const m = matById.get(r.labelId);
          if (m) m.visible = cb.checked;
        }
      }
    };
    lab.append(cb, ` ${g}`);
    el.append(lab);
  }
}

document.getElementById('load').onclick = () =>
  load(document.getElementById('jobId').value.trim(),
       document.getElementById('variantId').value.trim());

document.getElementById('transparent').onchange = (e) => {
  if (root) applyColors(e.target.checked);
};

(function animate() {
  requestAnimationFrame(animate);
  controls.update();
  renderer.render(scene, camera);
})();
```

- [ ] **Step 3: 수동 스모크(문서화)**

자동 테스트가 어려우므로 수동 절차를 남긴다. Task 7의 compose로 서버를 띄운 뒤:
1. 브라우저로 `http://localhost:8000`
2. jobId·variantId 입력 후 로드 → 뇌 영역이 색으로 렌더되는지
3. 반투명 셸 토글 → opacity 0.05로 바뀌는지
4. group 체크박스 → 해당 그룹 영역이 사라지는지

- [ ] **Step 4: 커밋한다**

```bash
git add seg_and_mesh/web/static/index.html seg_and_mesh/web/static/viewer.js
git commit -m "feat(web): three.js 단일 변형 뷰어(불투명/반투명·group 토글·색은 메타에서)"
```

---

## Task 7: compose + Dockerfile + .env.example

**Files:**
- Modify: `docker/api.Dockerfile` (CMD → uvicorn)
- Create: `docker-compose.yml`, `.env.example`
- Test: 수동 스모크(문서화)

**Interfaces:**
- Consumes: 전체
- Produces: `docker compose up`으로 뜨는 로컬 워크벤치

- [ ] **Step 1: Dockerfile CMD를 uvicorn으로 바꾼다**

`docker/api.Dockerfile`의 마지막 `CMD` 줄을 바꾼다. 또한 docker CLI를 넣어야 형제 FastSurfer 컨테이너를 띄울 수 있다. `apt-get install`에 `docker.io` 대신 docker CLI만: 정적 바이너리를 받는 대신 `docker-ce-cli`를 넣는다. 최소 변경으로, `apt-get`에 `ca-certificates curl gnupg`를 두고 docker CLI를 설치하는 대신, **호스트의 docker 소켓만 마운트하고 CLI는 이미지에 `docker.io`(client 포함) 패키지로 넣는다**:

`apt-get install` 목록에 `docker.io`를 추가한다(client CLI 제공). 그리고:

```dockerfile
ENV SAM_JOBS_ROOT=/work/jobs
CMD ["uv", "run", "--frozen", "uvicorn", "seg_and_mesh.web.server:app", \
     "--host", "0.0.0.0", "--port", "8000"]
```

이를 위해 `seg_and_mesh/web/server.py`를 만든다 — 환경변수에서 `AppConfig`를 만들어 `app`을 노출한다:

```python
"""uvicorn 진입점 — 환경변수에서 앱을 만든다 (스펙 §11)."""

from __future__ import annotations

import os
from pathlib import Path

from seg_and_mesh.web.app import AppConfig, create_app


def _config() -> AppConfig:
    image = os.environ.get("FASTSURFER_IMAGE")
    if not image:
        raise RuntimeError("FASTSURFER_IMAGE가 필요하다(.env, latest 금지)")
    return AppConfig(
        jobs_root=Path(os.environ.get("SAM_JOBS_ROOT", "/work/jobs")),
        fastsurfer_image=image,
        threads=int(os.environ.get("FASTSURFER_THREADS", "8")),
    )


app = create_app(_config())
```

- [ ] **Step 2: docker-compose.yml을 쓴다**

```yaml
# 로컬 단독 워크벤치 (스펙 §5.1, §11). 노출하지 않는다.
services:
  api:
    build:
      context: .
      dockerfile: docker/api.Dockerfile
    ports:
      - "127.0.0.1:${WEB_PORT:-8000}:8000"
    environment:
      FASTSURFER_IMAGE: ${FASTSURFER_IMAGE}
      FASTSURFER_THREADS: ${FASTSURFER_THREADS:-8}
      SAM_JOBS_ROOT: /work/jobs
      # 형제 FastSurfer 컨테이너의 -v 인자에 쓸 호스트 경로 (Task 1 to_host_path).
      # 도커 데몬은 호스트에 있어 api 내부의 /work/jobs를 모른다.
      SAM_HOST_JOBS_ROOT: ${OUTPUT_DIR}
    volumes:
      # 형제 FastSurfer 컨테이너를 띄우기 위한 docker socket (스펙 §5.1)
      - /var/run/docker.sock:/var/run/docker.sock
      # 잡 작업 폴더 — 호스트 OUTPUT_DIR에 바인드
      - ${OUTPUT_DIR}:/work/jobs
    # GPU는 api가 아니라 형제 FastSurfer 컨테이너가 docker run --gpus로 잡는다
```

- [ ] **Step 3: .env.example을 쓴다**

```bash
# 스펙 §11. 실제 .env는 커밋하지 않는다(.gitignore).
OUTPUT_DIR=/absolute/host/path/to/output
FASTSURFER_IMAGE=deepmi/fastsurfer:cuda-v2.5.4
FASTSURFER_THREADS=8
MAX_UPLOAD_MB=4096
MAX_EXTRACT_GB=20
WEB_PORT=8000
```

**형제 컨테이너 경로:** Task 1의 `to_host_path` + Task 4의 `host_*` 인자가 이걸 처리한다. compose가 `SAM_HOST_JOBS_ROOT=${OUTPUT_DIR}`를 주면 파이프라인이 FastSurfer의 `-v` 인자를 호스트 경로로 바꿔 넘긴다. `SAM_HOST_JOBS_ROOT`가 없으면(호스트에서 uvicorn 직접 실행) 변환 없이 그대로 쓴다.

`server.py`의 `_config()`가 이 값을 읽어 `AppConfig`에 실어야 한다:

```python
        host_jobs_root=(
            Path(os.environ["SAM_HOST_JOBS_ROOT"])
            if os.environ.get("SAM_HOST_JOBS_ROOT")
            else None
        ),
```

`AppConfig`에 `host_jobs_root: Path | None = None` 필드를 더하고, `run_segmentation_and_mesh`를 부를 때 `jobs_root=config.jobs_root, host_jobs_root=config.host_jobs_root`를 넘긴다(Task 5의 라우트).

- [ ] **Step 4: 수동 스모크(문서화)**

`docs/superpowers/notes/`에 절차를 남긴다:

```
1. cp .env.example .env  후 OUTPUT_DIR·FASTSURFER_IMAGE 채우기
2. docker compose up --build
3. http://localhost:8000 에서 실제 T1 DICOM zip 업로드
4. 시리즈 선택 → 실행 → 변형 로드 확인
5. 완료 후 docker compose down
```

- [ ] **Step 5: 커밋한다**

```bash
git add docker/api.Dockerfile docker-compose.yml .env.example seg_and_mesh/web/server.py
git commit -m "feat(web): compose·uvicorn 진입점·.env.example로 딸깍 기동"
```

---

## Self-Review 메모

- **세로 슬라이스 완결:** 업로드(T5)→게이트(T4·T5)→백그라운드 파이프라인(T4·T5)→변형 하나(T4)→뷰어(T6)→4파일(T4). compose로 딸깍(T7).
- **뼈대 5개 다 있음:** 잡 레이아웃(T1)·status 계약+PHI(T2)·regions-meta 조립(T3)·오케스트레이션(T4)·시리즈 게이트(T4·T5).
- **스펙 §6.3 게이트:** `ingest_job`이 awaiting_series에서 멈추고, 사용자가 `POST .../series`로 이어야 진행. 자동 시작 없음.
- **스펙 §12 PHI:** `sanitize_stderr`·`record_error`, upload 예외도 마스킹. 회귀 테스트 있음.
- **스펙 §2 4파일·§2.2 계약:** T3·T4. `space.toMNI152`는 명시적으로 제외(todo).
- **해결됨(2026-07-24 사전 점검):** 호스트/컨테이너 경로 변환은 `to_host_path`(T1)+`host_*` 인자(T4)+`SAM_HOST_JOBS_ROOT`(T7)로 처리. three.js는 vendor(T6).
- **미해결 todo(플랜에 명시):** 정본 확정(§6.6), 2-up 비교, MNI152, 스토리지 관리, 잡 취소, 변형 축 조합 UI.
- **type consistency:** `status.py`의 `now_iso`는 public. `pipeline.py`가 모듈 최상단에서 import한다(함수 안 import 금지).
- **낮은 테스트 커버리지 구역:** 뷰어 JS(T6)는 수동. compose(T7)는 수동. 나머지는 자동.
